{
  "name": "works-with-ofox",
  "type": "module",
  "version": "1.0.0",
  "description": "Ofox 应用展示墙 — 基于 Ofox 构建的应用精选列表",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-readme.js && node scripts/prepare-assets.js && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "validate": "node scripts/validate.js",
    "generate": "node scripts/generate-readme.js"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
